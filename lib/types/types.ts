export type LearningPath = {
  // img: string;
  // career: string;
  name: string | null;
  icon: string | null;
  id: number;
};

export interface User {
  id: number;
  name: string;
  email: string;
}
